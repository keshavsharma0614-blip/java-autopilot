# Application Configuration
spring:
  application:
    name: java-autopilot

# AI Configuration
jatp:
  ai:
    provider: openai # Options: openai, claude, ollama
    api-key: ${AI_API_KEY}
    model: gpt-4-turbo
    base-url: https://api.openai.com/v1

# Analysis Configuration
  analyzer:
    source-path: /Users/tanmaya/Desktop/OpenSource/java-autopilot/java-autopilot # Local path to the Java source code
    git-repo-url: null # If provided, the system will clone it first
